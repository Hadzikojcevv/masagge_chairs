import { MassageChairType } from "@/types/Types";

type SpecsPropsType = {
  chair: MassageChairType;
};

const Specs = () => {
  return <section className="specsSection">

    
  </section>;
};

export default Specs;
