export type MassageChairType = {
    id: number,
    name: string,
    subTitle: string,
    listItemDesc: string[],
    gallery: {
        product: string,
        headOn: string,
        side: string,
        zeroGravity: string
    }
}